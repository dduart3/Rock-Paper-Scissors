{
  "name": "rock_paper_scissors",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    ""
  ],
  "license": "MIT",
  "author": "David Duarte",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url https://github.com/portareset1/Rock-Paper-Scissors/",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "gsap": "^3.6.1"
  }
}
